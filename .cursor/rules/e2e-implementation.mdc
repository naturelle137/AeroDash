---
alwaysApply: false
---

- criticality: safety-critical aviation
- mode: strict correctness
- verbosity: no explanation unless asked
- feature-dir: `frontend/tests/e2e/features/{phase}/`
- step-path: `frontend/tests/e2e/steps/{name}.steps.ts`
- generated-path: `.features-gen/`
- generated-edit: forbidden
- step-file-count: 1 per domain group
- step-file-sharing: multi-feature allowed
- feature-without-steps: invalid
- phase-slugs: STC only
- technical-feature-dir: `frontend/tests/e2e/features/technical/`
- import-required: `import { expect } from '@playwright/test'`
- import-required: `import { createBdd } from 'playwright-bdd'`
- import-required: `const { Given, When, Then } = createBdd()`
- import-forbidden: BDD from `@playwright/test`
- import-forbidden: any `frontend/src/` import
- then-expect: required
- assertions: locator assertions only
- assertion-preferred: `toBeVisible`
- assertion-preferred: `toHaveText`
- assertion-absence: `.toHaveCount(0)`
- assertion-forbidden: `toBeTruthy`
- assertion-forbidden: `toBeDefined`
- wait-forbidden: `page.waitForTimeout`
- locator-order: `getByRole` > `getByLabel` > `getByText` > `data-testid` > CSS
- locator-forbidden: visual classes
- locator-forbidden: generated classes
- locator-forbidden: XPath
- css-usage: semantic only
- css-comment: required
- state-sharing: forbidden
- helpers: pure only
- step-calls: direct step-to-step forbidden
- business-e2e: real UI only
- business-e2e: mocking forbidden
- business-e2e: interception forbidden
- technical-e2e: `page.request` allowed
- technical-e2e: console checks allowed
- technical-e2e: full UI flow optional
- concern-mixing: forbidden
- orphan-steps: forbidden
- missing-implementations: forbidden
- step-match: exact
- step-match-case: case-sensitive
- step-params: `{string}` | `{int}` | `{float}` only
